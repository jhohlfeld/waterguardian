import { PropsWithChildren } from "react";

export const {{ pascalCase name }} = ({ children }: PropsWithChildren<{}>) => {
  return (
    <div>
      <h1>{{ pascalCase name }} Component</h1>
      {children}
    </div>
  );
};
